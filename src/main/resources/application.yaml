spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/springsecuritydb
    username: springsecurityuser
    password: spring-security

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

